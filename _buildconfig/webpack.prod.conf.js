// TODO: update to babel-plugin-lodash


/**
 * Production Only Webpack Settings
 * @type {{module: {rules: [*]}, plugins: [*]}}
 */
module.exports = {
    module: {
        rules: [],
    },

    plugins: [
    ],
};
